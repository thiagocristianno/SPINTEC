export interface Filme {
    id?: number,
    nome?: string,
    poster?: string,
    dtLancamento: Date,
    descricao?: string,
    ano?: string,
    nota: number,
    urlIMDb?: string,
    genero: string
}